\modeltable{chromaticity}{%
{\bf Chromaticity}. The rg-chromaticity, $(r,g,b)/\mathrm{max}(r,g,b)$,
obtained with each model. The measurements are interpolated using bicubic
spherical interpolation before being converted to chromaticity
values.\label{fig:chromaticity}}{\ignorespaces}

